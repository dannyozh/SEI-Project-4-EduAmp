<%= form_with(model: educator_profile, local: true) do |form| %>
  <% if educator_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(educator_profile.errors.count, "error") %> prohibited this educator_profile from being saved:</h2>

      <ul>
        <% educator_profile.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
